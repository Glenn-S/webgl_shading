import React from 'react';
import ReactDOM from 'react-dom';
import App from './components/App';

// Standard React boilerplate code entry point

ReactDOM.render(
  <App />, 
  document.querySelector('#root')
)